window.QS = window.QS || {};

(function($){
	var media = window.QS.media = {};

	/**
	 * =========================
	 * Utilities
	 * =========================
	 */

	_.extend(media, {
	});

	/**
	 * =========================
	 * Manager Hooks
	 * =========================
	 */

	_.extend(media, {
	});

	/**
	 * =========================
	 * jQuery Plugins
	 * =========================
	 */

})(jQuery);